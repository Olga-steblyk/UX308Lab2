//Take a variable with distance in kilometres as input and output the same distance converted to miles.

let x, y;
x = 7; //this is the input for the KM
y= x * 0.62137;

console.log(`${y} is the mile equivalent to ${x}km.`);
